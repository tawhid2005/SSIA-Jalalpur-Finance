# Automatic Github Push

Whenever you complete a task or make significant changes to the codebase, ALWAYS commit and push the changes to GitHub automatically.

Commands to run:
1. `git add .`
2. `git commit -m "Brief description of changes"`
3. `git push`
